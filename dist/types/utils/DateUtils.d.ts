export declare function isConsecutiveDays(date1: Date, date2: Date, tolerance?: number): boolean;
export declare function formatDate(date: Date): string;
export declare function getDateRange(dates: Date[]): {
    min: Date;
    max: Date;
};
